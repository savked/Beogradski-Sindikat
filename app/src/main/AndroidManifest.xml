<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sindikat.beogradski.beogradskisindikat">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SET_WALLPAPER" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.clanovi.ClanoviActivity" />
        <activity android:name=".ui.home.HomeActivity" />
        <activity
            android:name=".ui.oaplikaciji.OAplikaciji"
            android:label="@string/title_activity_oaplikaciji"
            android:theme="@style/AppTheme" />
        <activity android:name=".ui.slike.SlikeActivity" />
        <activity android:name=".ui.muzika.player.Slusanje" />
        <activity android:name=".ui.muzika.Albumi" />
        <activity android:name=".ui.muzika.pesme.Pesme"></activity>
    </application>

</manifest>